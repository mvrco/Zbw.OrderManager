<mah:MetroWindow x:Class="ZbW.ITB1821H.OrderManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ZbW.ITB1821H.OrderManager"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <mah:MetroWindow.ContextMenu>
        <ContextMenu>
            <MenuItem Command="ApplicationCommands.New" />
            <MenuItem Command="ApplicationCommands.Delete" />
        </ContextMenu>
    </mah:MetroWindow.ContextMenu>
    <Grid>
        <DockPanel>
            <StatusBar DockPanel.Dock="Bottom" x:Name="StatusBar">
                <StatusBarItem DockPanel.Dock="Right">
                    <Slider x:Name="zoomSlider" Width="125" Value="1" Minimum=".5" Maximum="2" Style="{DynamicResource MahApps.Styles.Slider.Win10}" Foreground="DarkBlue" />
                </StatusBarItem>
                <StatusBarItem>
                    <TextBlock Text="{Binding StatusText}" />
                </StatusBarItem>
            </StatusBar>
            <Expander DockPanel.Dock="Left" ExpandDirection="Right" IsExpanded="True" Padding="2" Background="{Binding ElementName=StatusBar, Path=Background}">
                <Expander.Header>
                    <TextBlock Text="Customers" FontSize="13" FontWeight="DemiBold">
                        <TextBlock.LayoutTransform>
                            <RotateTransform Angle="90" />
                        </TextBlock.LayoutTransform>
                    </TextBlock>
                </Expander.Header>
                <!--<uc:SideBar />-->
            </Expander>
            <TabControl x:Name="tabs" Grid.Column="2" Margin="5 0">
                <TabControl.LayoutTransform>
                    <ScaleTransform ScaleX="{Binding ElementName=zoomSlider, Path=Value}" ScaleY="{Binding ElementName=zoomSlider, Path=Value}" />
                </TabControl.LayoutTransform>
            </TabControl>
        </DockPanel>
    </Grid>
</mah:MetroWindow>